#include <stdio.h>
#include <stdlib.h>


char * unique_char( char * input_str ){
    return NULL;
}

int main(){
    char * c = "helloworld";
    printf("word before: %s\n", c);
    printf("word after: %s\n", unique_char( c ) );
}
